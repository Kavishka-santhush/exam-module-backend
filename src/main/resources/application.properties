# Application Properties
spring.application.name=short-courses-examinations-backend
server.port=2000

# Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/short_courses_examinations
spring.datasource.username=postgres
spring.datasource.password=postgres
spring.datasource.driver-class-name=org.postgresql.Driver

# Hibernate JPA Configuration
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
# Hibernate ddl auto (create, create-drop, validate, update)
spring.jpa.show-sql=true
spring.jpa.open-in-view=false

# Connection Pool Settings (HikariCP)
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.idle-timeout=30000
spring.datasource.hikari.connection-timeout=20000
spring.datasource.hikari.max-lifetime=1800000


#JWT Configuration
ousl.app.jwtSecret= OUSLShortCourseExaminationsModule-glhGhSsp2McS9n78DFP3
ousl.app.jwtExpirationMs= 86400000